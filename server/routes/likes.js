// Author: Alec Moldovan
// Description: Contain all CRUD operations effecting the likes table.
import express from 'express';
import async from 'async';
import pool from '../helpers/database.js';

const router = express.Router();

const like = router.put("like", async (req, res) =>{





})
const unlike = router.put("unlike", async (req, res) =>{




})

export { like, unlike };

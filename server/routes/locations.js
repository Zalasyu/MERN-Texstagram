// Author: Alec Moldovan
// Description: Contain all CRUD operations effecting the locations table.
import express from 'express';
import async from 'async';
import pool from '../helpers/database.js';

const createLocation = router.put("comment", async (req, res) =>{





});

const deleteLocation = router.put("uncomment", async (req, res) =>{




});

const editLocation = router.put("editComment", async (req, res) => {




});


export { createLocation, deleteLocation, editLocation };
